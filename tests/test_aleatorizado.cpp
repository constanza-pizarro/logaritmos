#include "../aleatorizado.h"

int main() {
    int n=8;

    double result = randomizedAlgorithm(n);

    cout << "La distancia mínima entre los puntos en los pares es: " << result << endl;

    return 0;
}